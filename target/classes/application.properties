logging.level.com.api.films=DEBUG
spring.output.ansi.enabled=ALWAYS

#Porta que est� rodando o projeto
server.port=8080
#Configura��o do  caminho para o banco de dados usando PostgreSQL
# Database
spring.datasource.url=jdbc:postgresql://localhost:5432/filmesdb
spring.datasource.username=postgres
spring.datasource.password=18263269

# Driver
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# Esta � a chave secreta que ser� usada para assinar nossos Tokens JWT
# Em um projeto real, isso deve ser uma string MUITO complexa e segura
api.security.token.secret=troque-esta-chave-por-uma-muito-maior-e-segura-2025-KEY
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Para ver os logs